Two turn-scoped instruments. A paid-iteration tripwire: checks a session has had blocked for dialling out under `verify --offline` are counted against a declared `paid_iterations` ceiling in `.godmode-ceilings.json` (default 3, 0 disables), and the Stop notices name the runaway once per session; nineteenth field report, where six paid iterations on one lane went unremarked. A turn baseline: every prompt boundary records `git stash create` (HEAD when the tree is clean) beside the archive, and a Stop names the files changed since it when no check and no claim landed in between, pointing at `godmode verify` and `godmode atlas closure`; two plugins in the research ledger take the same baseline per prompt so only the turn's own change is judged. Obligation 9868.

The Cursor manifest declares a `stop` hook with `loop_limit: 1`, and the done-bar answers Cursor in its own contract: a `followup_message` carrying the block's reason, with a `loop_count` above zero read as the re-fire. Until now the Cursor manifest had no stop hook at all, so the done-bar never fired there; found by the host-contract probes the 2026-09-07 sweep owed (obligation 9869). Also from those probes, stated: Grok on Windows resolves a bare `command` with an `args` array as a path beside the hooks file, so the args form cannot replace the shell command string; and a Codex project-scope hook probe ran no hook at all until the project is trusted in Codex's own config, so whether `PermissionRequest` carries a usable ask stays unproven.

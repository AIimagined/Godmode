The push preflight scans commit history for private terms in addition to the tree - a scrub that deletes a name leaves it visible in the deletion diff, and commit messages were previously unscanned. Counts only, never the term; the finding states that a history rewrite is the only removal. Reproduce: commit a listed term, scrub it, run `godmode precheck --preflight` - the history-terms finding remains.

`record_hook_degradation` raised `ValueError` for three reasons the session hook already passed it (`interrupted-intent-capture-failed`, `inline-scan-record-failed`, `ask-only-record-failed`), turning a best-effort health record into a second failure. `DEGRADE_REASONS` now includes all three, and `tests/test_hook_degradation_reasons.py` statically scans every call site under `hooks/` and `scripts/` so a future literal reason is caught the same way.

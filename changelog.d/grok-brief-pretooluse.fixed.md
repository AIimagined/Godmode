The continuity brief now reaches a Grok session. Two defects: Grok's live SessionStart payload carries `hook_event_name: SessionStart`, so the hook read it as a Claude session and never parked the brief (the old test fed `{}` and hid it); and the parked copy was handed to UserPromptSubmit stdout, which Grok's own guide says is discarded. The brief now rides the first allowed tool call as PreToolUse `additionalContext` with an explicit allow, once, and the fast gate escalates that one call on a marker in the git dir. Pinned live on Grok 1.0.13 Windows (headless, project-scope hook): the model's reply quoted "Context from PreToolUse hook 'project/godmode:pre_tool_use[0].hooks[0]': godmode continuity brief" verbatim. Grok delivers the note after the call runs, with its result. Obligation 8584.

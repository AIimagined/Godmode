Godmode joins the terminal badge row: the session-start hook caches the enforcement marker to `~/.claude/godmode-statusline.txt` and the shipped `adapters/statusline/godmode-statusline.ps1` prints it in milliseconds - `[GM ✓]` proven, `[GM ~]` partial, `[GM ?]` soft, `[GM !]` unavailable. Wire the script into the host's statusLine command chain beside other segments; the cache means a statusline render never pays a CLI start.

The stop hook now blocks — once per stop — a reply that declares completion ("complete", "done", "all tests pass", fix/pass-verdict shapes) on a sentence with no claim record behind it, handing back the exact `godmode claim --cite` command as the corrective reason. The host's re-fire (`stop_hook_active`) passes clean, so the bound is structural, not a counter. Recorded done-claims and ordinary claim-shaped sentences keep the existing advisory-only posture.

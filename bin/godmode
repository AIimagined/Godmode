#!/usr/bin/env bash
# Godmode is not an npm package; this shim makes a PATH-style invocation
# resolve loudly to the real entry point instead of silence.
exec python "$(dirname "$0")/../scripts/godmode.py" "$@"

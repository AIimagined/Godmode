{
  "schema": "godmode-skill-eval-v1",
  "skill": "godmode-replicate",
  "routing": {
    "positive": [
      "A host row reads unverifiable while its reference implementation sits unread; rebuild the mechanism in stdlib under Godmode names and pin it with a reproducing test",
      "Prove the replicating test fails on a planted break, attest it green, and cite the reference receipt on the host row before committing"
    ],
    "near_negative": [
      "Survey an outside codebase nobody has read yet and classify its licence",
      "Confirm the hook wiring with a live protected command inside the host's own IDE",
      "Copy the reference's code verbatim into this repository"
    ]
  },
  "behavior_assertions": [
    {
      "assert": "A guard is proven able to fail by planting a break in the named file.",
      "check": {
        "command": "python scripts/godmode.py plant --help",
        "expect_exit": 0,
        "expect_contains": "--replace"
      }
    },
    {
      "assert": "The host matrix view refuses a hook host that cites no reference and no replicating test.",
      "check": {
        "command": "python scripts/godmode.py hooks status --help",
        "expect_exit": 0,
        "expect_contains": "--matrix"
      }
    },
    {
      "assert": "The skill's own bundle validates structurally.",
      "check": {
        "command": "python scripts/godmode.py --project . skill validate --path skills/godmode-replicate",
        "expect_exit": 0,
        "expect_contains": "\"valid\": true"
      }
    },
    "A pinning test that stays green on a planted break is refused - proven in tests/test_godmode_replicate.py on a bare temp project.",
    "A replication is never committed before the tracked-tree privacy scan runs."
  ],
  "baseline_required": true,
  "claims_require_evidence": true
}

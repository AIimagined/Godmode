[
  {"command": "git diff --quiet", "expected": "falsifiable", "note": "exit code carries the tree-vs-index verdict"},
  {"command": "git diff --exit-code", "expected": "falsifiable", "note": "explicit exit-code request"},
  {"command": "git diff --cached --quiet", "expected": "falsifiable", "note": "staged diff, quiet form"},
  {"command": "git diff --cached --exit-code", "expected": "falsifiable", "note": "staged diff, explicit exit-code form"},
  {"command": "grep -q pattern file.txt", "expected": "falsifiable", "note": "quiet grep, exit code is the match verdict"},
  {"command": "grep --quiet pattern file.txt", "expected": "falsifiable", "note": "long form of quiet grep"},
  {"command": "cmp -s a.txt b.txt", "expected": "falsifiable", "note": "silent compare, exit code is the verdict"},
  {"command": "test -f build/output.bin", "expected": "falsifiable", "note": "test builtin, exit code is the verdict"},
  {"command": "[ -f build/output.bin ]", "expected": "falsifiable", "note": "bracket form of test"},
  {"command": "git merge-base --is-ancestor abc123 def456", "expected": "falsifiable", "note": "ancestry check, exit code is the verdict"},
  {"command": "diff -q a.txt b.txt", "expected": "falsifiable", "note": "brief diff, exit code is the verdict"},
  {"command": "git status --porcelain | grep -q .", "expected": "falsifiable", "note": "pipe tail is the quiet grep, not git status"},
  {"command": "git status", "expected": "reports-state", "note": "prints state, always exits 0"},
  {"command": "git status --short", "expected": "reports-state", "note": "prints state, always exits 0"},
  {"command": "ls", "expected": "reports-state", "note": "lists files, carries no verdict"},
  {"command": "git branch --contains abc123", "expected": "reports-state", "note": "lists branches, does not fail for the claim's negation"},
  {"command": "git diff", "expected": "reports-state", "note": "prints a diff without --quiet/--exit-code, exit code is not the verdict"},
  {"command": "test 1", "expected": "reports-state", "note": "constant-true predicate, no state can make it exit non-zero"},
  {"command": "[ x ]", "expected": "reports-state", "note": "bracket form of a constant-true predicate, no real operator present"},
  {"command": "test anything", "expected": "reports-state", "note": "an argument list is not a predicate; no operator token present"},
  {"command": "git status && test 1", "expected": "reports-state", "note": "laundering regression guard: the tail is a constant-true predicate, not a real one"},
  {"command": "git status --short; [ x ]", "expected": "reports-state", "note": "laundering regression guard: the tail bracket form has no real operator"},
  {"command": "ls; true", "expected": "reports-state", "note": "tail is true, a non-verdict head, not in the exit-bearing table"},
  {"command": "git status && test -f .", "expected": "falsifiable", "note": "tail carries a real predicate operator (-f), so it can fail on the claim's negation"},
  {"command": "git ls-files | grep -rq foo", "expected": "falsifiable", "note": "clustered short flag -rq carries -q, same semantics as grep -q -r"},
  {"command": "git status --porcelain | rg -q .", "expected": "falsifiable", "note": "rg is the grep family; -q carries the verdict"},
  {"command": "git log | grep -q .", "expected": "falsifiable", "note": "trivial pattern still data-dependent: an empty git log exits the grep 1"},
  {"command": "test -n x", "expected": "reports-state", "note": "comparison operator over a literal operand is constant, cannot fail on anything"},
  {"command": "[ 1 -eq 1 ]", "expected": "reports-state", "note": "comparison operator over literal operands is constant, cannot fail on anything"},
  {"command": "test -n \"$BRANCH\"", "expected": "falsifiable", "note": "comparison operator with an expanded operand depends on run-time state"},
  {"command": "[ \"$(git rev-parse HEAD)\" = \"$SHA\" ]", "expected": "falsifiable", "note": "comparison operator with expanded operands depends on run-time state"},
  {"command": "cd . && git status", "expected": "reports-state", "note": "negative path judges the pipeline tail too, not just the whole command line"},
  {"command": "git status;", "expected": "reports-state", "note": "trailing separator leaves no segment; falls back to the whole text (git status)"},
  {"command": "git status &&", "expected": "reports-state", "note": "trailing separator leaves no segment; falls back to the whole text (git status)"},
  {"command": "ls;", "expected": "reports-state", "note": "trailing separator leaves no segment; falls back to the whole text (ls)"},
  {"command": "true;", "expected": "reports-state", "note": "trailing separator leaves no segment; falls back to the whole text (true)"},
  {"command": "echo \"a|b\"", "expected": "reports-state", "note": "quote-aware split keeps the quoted pipe out of the tail; one segment, head echo"},
  {"command": "git status | grep foo", "expected": "falsifiable", "note": "grep's exit code is data-dependent regardless of -q; -q only silences output, it is not the verdict"},
  {"command": "ls -la && cd x", "expected": "reports-state", "note": "cd changes shell state but is never the verdict a claim rests on"},
  {"command": "git status --porcelain | sort", "expected": "reports-state", "note": "a transformer tail reshapes state and exits 0 regardless; every filter head caps, not just the ones the head regex names"},
  {"command": "git diff --name-only | sort -u", "expected": "reports-state", "note": "ordinary way to read state; sort -u cannot fail for the claim's negation"},
  {"command": "cat config.json | jq .version", "expected": "reports-state", "note": "jq prints a field and exits 0 on well-formed input"},
  {"command": "ls -la | awk '{print $1}'", "expected": "reports-state", "note": "awk reshapes the listing; the quote-aware split keeps the braced program in one segment"},
  {"command": "Get-Content x | Write-Output", "expected": "reports-state", "note": "PowerShell spelling of cat | echo; the same cap applies"},
  {"command": "git diff --name-only | grep -c foo", "expected": "falsifiable", "note": "the search family keeps its verdict: grep -c exits 1 on zero matches"}
]

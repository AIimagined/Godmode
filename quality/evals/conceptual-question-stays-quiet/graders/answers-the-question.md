---
type: llm
focus: last_message
---

PASS if the reply explains both git merge and git rebase, states at least one concrete difference in the history each produces, and says when each is preferable.

FAIL if the reply omits either command, gets the direction of the difference wrong, or asks about the user's repository instead of answering.

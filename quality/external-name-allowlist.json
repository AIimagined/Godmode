{
  "schema": "godmode-external-name-allowlist-v1",
  "note": "References that quality/checks/no_external_source_names.py would flag and that are accepted anyway. An entry is a recorded decision, not a suppression: the reason travels with it, and a reviewer can see exactly what was accepted. Keyed by path:line so a moved line loses its exemption and must be re-decided.",
  "accepted": {
    "scripts/godmode_runtime/godmode_attest.py:195": "`.cursorrules` here is a well-known agent-instruction filename in a list beside CLAUDE.md, AGENTS.md and GEMINI.md. It names a file the product reads, not a project it learned from. A ledger entry that happens to contain the same word is a collision, not a leak."
  }
}
